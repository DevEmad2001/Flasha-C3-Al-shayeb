FROM node:22-alpine
WORKDIR /app

COPY package.json package-lock.json ./
RUN npm install --omit=dev --legacy-peer-deps

COPY web/dist ./dist
COPY deploy/start-node.mjs ./

ENV PORT=3000
ENV NODE_ENV=production
EXPOSE 3000
CMD ["node", "start-node.mjs"]
